# tp-final-sor2

1. Parseo de archivo XML a CSV: Entrar a *src/* y realizar el comando "*python parser.py -i [RUTA DEL ARCHIVO]*", este programa leera el archivo .xml y lo parseara a CSV. *Tambien se puede utilizar "*python parser.py -h*" para explicar cada parametro.
2. Se recomienda realizar el siguiente comando temporal para probar el funcionamiento de "*export NVD_KEY = [API_KEY]*"
3. Ejecutar el comando "*python etiquetador_nvd.py -i hallazgos_nmap.csv -o dataset_final.csv -k $NVD_KEY*"
